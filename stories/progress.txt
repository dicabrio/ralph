## Codebase Patterns (Updated by Agents)
- TanStack Start with file-based routing in src/routes/
- Component files use PascalCase (e.g., Header.tsx)
- TailwindCSS v4 via @tailwindcss/vite plugin
- Path alias @ configured for src/ folder in vite.config.ts
- Lucide React for icons
- Root layout in src/routes/__root.tsx with RootDocument shell component
- Layout component wraps all routes with responsive sidebar navigation
- Sidebar component in src/components/Sidebar.tsx with collapse toggle and mobile overlay
- Dark mode via system preference (prefers-color-scheme media query)
- Dev server runs on port 9000
- Project uses pnpm (v8.x) with lockfile v6 - use pnpm@8.15.9 for Docker builds
- Docker build uses multi-stage build: deps → builder → runner
- TanStack Start builds to .output/ folder, run with `node .output/server/index.mjs`
- Docker health checks use 127.0.0.1 (not localhost) to avoid IPv6 issues in Alpine
- Docker socket mount pattern for Docker-out-of-Docker (DooD) to spawn Claude containers
- Helper scripts in scripts/ folder for Claude container management
- HOST_PROJECTS_ROOT and HOST_SKILLS_PATH env vars needed for mounting host paths in spawned containers
- SQLite database at {DATA_PATH}/ralph.db with better-sqlite3 driver
- Database schema files in src/db/schema/ folder with barrel export in index.ts
- Drizzle migrations in drizzle/ folder, run with `pnpm db:generate` and `pnpm db:migrate`
- WAL mode enabled for better concurrent SQLite performance
- Unit tests in src/**/*.test.ts, E2E tests in e2e/*.spec.ts
- Vitest with jsdom environment for React component testing
- Playwright with chromium for E2E testing (auto-starts dev server)
- Test coverage via @vitest/coverage-v8 with v8 provider
- tRPC v11 with superjson transformer at /api/trpc/*
- tRPC routers in src/lib/trpc/routers/ with barrel export
- tRPC integrated with TanStack Query via trpc.Provider in root-provider.tsx
- tRPC router tests use createCallerFactory pattern for isolated testing
- Mock node:fs with importOriginal to preserve DB initialization while mocking project paths
- Services in src/lib/services/ folder for business logic (projectDiscovery, runnerManager)
- path.join() strips leading ./ - use consistent path formats when comparing discovered vs database paths

## 2026-02-06 - SETUP-001
**Title:** TanStack Start project initialisatie

**Implemented:**
- Verified TanStack Start project with TypeScript is already configured
- Verified TailwindCSS v4 is working via @tailwindcss/vite plugin
- Verified folder structure exists: src/routes, src/components, src/lib, src/db
- Updated dev server port from 3000 to 9000 in package.json
- Verified Header.tsx component provides navigation (slide-out sidebar menu)

**Files changed:**
- package.json (dev script port changed to 9000)

**Learnings:**
- Project was mostly set up already via TanStack Start template
- TailwindCSS v4 uses @tailwindcss/vite plugin instead of postcss config
- The Header component already contains a comprehensive navigation sidebar
- TanStack Router devtools and Query devtools are integrated
- Better-auth is already configured for authentication
- Drizzle ORM is set up for database operations

## 2026-02-06 - SETUP-002
**Title:** Docker configuratie

**Implemented:**
- Created multi-stage Dockerfile for Node.js/pnpm TanStack Start application
- Created docker-compose.yml with ralph-app service
- Configured volume mounts: DATA_PATH, PROJECTS_ROOT, SKILLS_PATH
- Set up environment variables: ANTHROPIC_API_KEY, PROJECTS_ROOT, SKILLS_PATH, DATA_PATH
- Created .dockerignore for optimized builds
- Created .env.example with documentation
- Added health check with 127.0.0.1 (IPv4) to avoid Alpine IPv6 issues

**Files changed:**
- Dockerfile (new)
- docker-compose.yml (new)
- .dockerignore (new)
- .env.example (new)
- .gitignore (added data/, projects/, skills/ to ignore list)
- stories/prd.json (status updated)

**Learnings:**
- pnpm lockfile v6 requires pnpm 8.x, use specific version (8.15.9) in Dockerfile
- TanStack Start with nitro builds to .output/ folder
- Alpine wget uses IPv6 first for localhost, causing health check failures - use 127.0.0.1
- Build output is started with `node .output/server/index.mjs`
- Better-auth requires BETTER_AUTH_SECRET and BETTER_AUTH_BASE_URL env vars (for later stories)

## 2026-02-06 - SETUP-003
**Title:** Claude Docker integratie voorbereiden

**Implemented:**
- Added Docker socket mount to docker-compose.yml for Docker-out-of-Docker (DooD) pattern
- Installed Docker CLI in runner stage of Dockerfile
- Created claude-runner reference service in docker-compose with manual profile
- Created helper scripts for Claude container management:
  - start-claude-runner.sh: Start a Claude container for a specific project
  - stop-claude-runner.sh: Stop and remove a running Claude container
  - claude-runner-status.sh: Check status of Claude runner containers
- Added HOST_PROJECTS_ROOT and HOST_SKILLS_PATH env vars for host path resolution
- Verified Docker CLI works from within Ralph container

**Files changed:**
- docker-compose.yml (added Docker socket mount, claude-runner reference, HOST env vars)
- Dockerfile (added docker-cli installation, scripts copy)
- .dockerignore (added !scripts to include scripts folder)
- scripts/start-claude-runner.sh (new)
- scripts/stop-claude-runner.sh (new)
- scripts/claude-runner-status.sh (new)
- stories/prd.json (status updated)

**Learnings:**
- Docker-out-of-Docker (DooD) is simpler and safer than Docker-in-Docker (DinD)
- Docker socket mount allows spawning containers on the host from within a container
- Host paths must be passed explicitly (HOST_PROJECTS_ROOT) because container paths differ from host paths
- Alpine's docker-cli package is lightweight (~15MB) and sufficient for container management
- Using profiles: [manual] in docker-compose prevents a service from starting with `docker-compose up`

## 2026-02-06 - DB-001
**Title:** SQLite database setup met Drizzle ORM

**Implemented:**
- Updated drizzle.config.ts to use DATA_PATH env var for database location
- Updated src/db/index.ts with proper better-sqlite3 initialization and DATA_PATH handling
- Created schema folder structure: src/db/schema/ with common.ts, todos.ts, index.ts
- Enabled WAL mode for better concurrent SQLite performance
- Auto-creates data directory if it doesn't exist
- Added test script scripts/test-db.ts for verifying database connection
- Generated first migration for todos table

**Files changed:**
- drizzle.config.ts (updated to use DATA_PATH, schema folder path, enabled strict mode)
- src/db/index.ts (complete rewrite with better-sqlite3 initialization, WAL mode, directory creation)
- src/db/schema.ts (deleted)
- src/db/schema/common.ts (new - reusable timestamp columns)
- src/db/schema/todos.ts (new - moved from schema.ts)
- src/db/schema/index.ts (new - barrel export)
- src/routes/demo/drizzle.tsx (updated import path)
- scripts/test-db.ts (new - database connection test)
- drizzle/ (new - generated migrations folder)
- stories/prd.json (status updated)

**Learnings:**
- better-sqlite3 is synchronous, unlike async database drivers
- Database path should be built with path.join(), not string concatenation
- better-sqlite3 requires the database file path directly, not a URL like postgres
- WAL mode (Write-Ahead Logging) improves concurrent read/write performance
- Drizzle's execute() method is not available for better-sqlite3 driver, use sqlite.prepare() instead
- Schema folder structure allows for future expansion with separate table files
- Default DATA_PATH=./data works for local development without Docker
- Unit tests in src/**/*.test.ts, E2E tests in e2e/*.spec.ts
- Vitest with jsdom environment for React component testing
- Playwright with chromium for E2E testing (auto-starts dev server)
- Test coverage via @vitest/coverage-v8 with v8 provider

## 2026-02-06 - SETUP-004
**Title:** Test setup (Vitest, RTL, Playwright)

**Implemented:**
- Configured Vitest with vitest.config.ts (jsdom environment, globals, coverage)
- Created test setup file src/test/setup.ts with RTL cleanup and jest-dom matchers
- Installed and configured Playwright for E2E tests with chromium
- Added test scripts: test, test:unit, test:e2e, test:coverage, test:watch
- Created example unit test for cn() utility (6 passing tests)
- Created E2E tests for home page (3 passing tests)
- Added typecheck script for TypeScript validation
- Updated tsconfig.json with vitest/globals types
- Updated .gitignore for test output folders

**Files changed:**
- vitest.config.ts (new)
- playwright.config.ts (new)
- src/test/setup.ts (new)
- src/lib/utils.test.ts (new - example unit test)
- e2e/home.spec.ts (new - example E2E test)
- package.json (added test scripts, installed test dependencies)
- tsconfig.json (added vitest/globals types)
- .gitignore (added coverage/, test-results/, playwright-report/)
- stories/prd.json (status updated)

**Learnings:**
- Vitest version must match @vitest/coverage-v8 version (both 3.x or both 4.x)
- React Testing Library needs cleanup in afterEach for proper test isolation
- Playwright's webServer config auto-starts dev server for E2E tests
- Use getByRole() in Playwright to avoid strict mode violations with multiple elements
- CI=true enables headless mode and single worker in Playwright
- Pre-existing TypeScript errors in demo files don't affect test functionality

## 2026-02-06 - DB-002
**Title:** Schema: projects tabel

**Implemented:**
- Created src/db/schema/projects.ts with complete table definition
- Fields: id (autoincrement PK), name, path (unique), description, branch_name, created_at, updated_at
- Used reusable timestamps from common.ts for consistency
- Added unique constraint on path field
- Exported from barrel file src/db/schema/index.ts
- Generated migration: drizzle/0001_tiny_bushwacker.sql
- Applied migration successfully

**Files changed:**
- src/db/schema/projects.ts (new)
- src/db/schema/index.ts (added projects export)
- drizzle/0001_tiny_bushwacker.sql (new - auto-generated migration)
- stories/prd.json (status updated)

**Learnings:**
- SQLite uses `text` type for strings (no varchar with length like PostgreSQL)
- Drizzle generates migrations with readable SQL including CREATE TABLE and CREATE UNIQUE INDEX
- SQLite unique constraints are implemented as unique indexes
- Schema follows existing pattern: sqliteTable, integer with mode, text, spread timestamps
- Type exports (Project, NewProject) use $inferSelect and $inferInsert for type safety
- Pre-existing TypeScript errors in drizzle-orm node_modules don't affect actual schema functionality
- Foreign keys with onDelete: 'cascade' for automatic cleanup of related records

## 2026-02-06 - DB-004
**Title:** Schema: runner_logs tabel

**Implemented:**
- Created src/db/schema/runner_logs.ts with complete table definition
- Fields: id, project_id (FK), story_id, log_content, log_type (stdout/stderr), timestamp
- Foreign key to projects table with cascade delete
- Added 3 indexes: project_id, timestamp, and composite (project_id, timestamp)
- Exported from barrel file src/db/schema/index.ts
- Generated migration: drizzle/0002_nervous_valeria_richards.sql
- Applied migration successfully

**Files changed:**
- src/db/schema/runner_logs.ts (new)
- src/db/schema/index.ts (added runner_logs export)
- drizzle/0002_nervous_valeria_richards.sql (new - auto-generated migration)
- stories/prd.json (status updated)

**Learnings:**
- Drizzle's index() function uses array return syntax in table definition callback: `(table) => [index(...), ...]`
- Composite indexes are useful for queries that filter on multiple columns (e.g., logs by project and time)
- text() with enum option creates a SQLite text column with CHECK constraint for allowed values
- story_id is nullable since some logs may be general project logs not tied to a specific story
- SQLite foreign keys with references() and onDelete: 'cascade' handle parent deletion gracefully

## 2026-02-06 - API-001
**Title:** tRPC setup met TanStack Start

**Implemented:**
- Installed tRPC v11 (@trpc/server, @trpc/client, @trpc/react-query) with superjson transformer
- Created tRPC server configuration in src/lib/trpc/trpc.ts with router, publicProcedure, middleware
- Set up API handler at src/routes/api/trpc/$.ts using fetchRequestHandler
- Integrated tRPC client with existing TanStack Query in root-provider.tsx
- Created health router with check (status, timestamp, uptime) and echo endpoints
- Added demo page at /demo/trpc showing type-safe queries
- Unit tests for health router (7 passing tests)

**Files changed:**
- package.json (added tRPC dependencies)
- pnpm-lock.yaml (updated)
- src/lib/trpc/trpc.ts (new - server configuration)
- src/lib/trpc/client.ts (new - client configuration)
- src/lib/trpc/context.ts (new - context factory)
- src/lib/trpc/index.ts (new - barrel export)
- src/lib/trpc/routers/index.ts (new - app router)
- src/lib/trpc/routers/health.ts (new - health endpoints)
- src/lib/trpc/routers/health.test.ts (new - unit tests)
- src/routes/api/trpc/$.ts (new - API handler)
- src/routes/demo/trpc.tsx (new - demo page)
- src/integrations/trpc/provider.tsx (new - provider component)
- src/integrations/tanstack-query/root-provider.tsx (updated with tRPC provider)

**Learnings:**
- TanStack Start API routes use createFileRoute with server.handlers for GET/POST
- tRPC fetchRequestHandler works seamlessly with TanStack Start's request handling
- superjson automatically handles Date serialization/deserialization between client and server
- tRPC client can share the same QueryClient as TanStack Query for unified caching
- createCallerFactory is useful for testing routers without HTTP requests
- File-based routing creates type definitions in routeTree.gen.ts automatically
- WebSocket server with ws library in src/lib/websocket/
- WebSocket Vite plugin starts WS server on port 9001 during development
- Use `@vitest-environment node` comment for tests that need Node.js (not jsdom)

## 2026-02-06 - API-002
**Title:** Websocket server voor live logs

**Implemented:**
- Created WebSocket server with `ws` library at src/lib/websocket/server.ts
- Implemented project subscription system: subscribe/unsubscribe by projectId
- Server broadcasts log updates to subscribed clients only
- Log buffer (100 lines per project) for late-joining clients
- Heartbeat mechanism with 30s interval for connection health monitoring
- React client hook at src/lib/websocket/client.ts with:
  - Automatic reconnection with exponential backoff and jitter
  - Message queueing during disconnection
  - Connection state management (isConnected, isReconnecting)
- Vite plugin to start WebSocket server during development (port 9001)
- Unit tests for WebSocket server (11 tests passing)

**Files changed:**
- package.json (added ws, @types/ws dependencies)
- pnpm-lock.yaml (updated)
- src/lib/websocket/types.ts (new - shared types for WS messages)
- src/lib/websocket/server.ts (new - WebSocket server implementation)
- src/lib/websocket/client.ts (new - React hook with reconnection)
- src/lib/websocket/vite-plugin.ts (new - Vite dev server integration)
- src/lib/websocket/index.ts (new - barrel export)
- src/lib/websocket/server.test.ts (new - 11 unit tests)
- vite.config.ts (added webSocketPlugin)
- stories/prd.json (status updated)

**Learnings:**
- TanStack Start with Nitro uses Fetch API which doesn't expose raw sockets for WebSocket upgrades
- Separate WebSocket server on port 9001 is the cleanest solution for this architecture
- Vite plugins can start background services via `configureServer()` hook
- WebSocketServer standalone mode doesn't support path filtering; path is only used with noServer mode
- Tests running real WebSocket connections need `@vitest-environment node` (not jsdom)
- Use random ports in tests to avoid conflicts when tests run in parallel
- Message collection pattern with waiters array helps avoid race conditions in async tests
- ws library's Buffer type needs explicit handling in TypeScript for message parsing
- tRPC routers use createCallerFactory for unit testing without HTTP
- Mock node:fs with importOriginal to preserve database functionality while mocking project paths

## 2026-02-06 - API-003
**Title:** Projects API routes

**Implemented:**
- Created tRPC router at src/lib/trpc/routers/projects.ts with full CRUD operations
- Procedures: list, getById, create, update, delete
- create/update validates that path exists on filesystem via existsSync
- Sync with prd.json when fetching projects (reads projectDescription, branchName from file)
- Input validation with Zod schemas (createProjectSchema, updateProjectSchema)
- TRPCError handling for NOT_FOUND, BAD_REQUEST, and CONFLICT errors
- Unit tests with mocked filesystem (21 tests passing)

**Files changed:**
- src/lib/trpc/routers/projects.ts (new - projects tRPC router)
- src/lib/trpc/routers/projects.test.ts (new - 21 unit tests)
- src/lib/trpc/routers/index.ts (added projects router to appRouter)
- stories/prd.json (status updated)

**Learnings:**
- When mocking node:fs in tests, use `importOriginal` to preserve unmocked functions (mkdirSync, etc.)
- Database initialization uses existsSync/mkdirSync, so partial mocks must allow these through
- SQLite unique constraint violations throw error with "UNIQUE constraint failed" message
- Drizzle's .returning() returns an array, always destructure first element for single row
- prd.json sync pattern: read file on every getById/list, update DB if values differ
- Use passthrough() in Zod schemas to allow unknown properties in parsed JSON files
- TRPCError codes: NOT_FOUND (404), BAD_REQUEST (400), CONFLICT (409)
- Stories router reads/writes prd.json for story management
- Use `--no-file-parallelism` for unit tests to avoid SQLite concurrent access issues

## 2026-02-06 - API-004
**Title:** Stories API routes

**Implemented:**
- Created tRPC router at src/lib/trpc/routers/stories.ts for story management
- Procedures: listByProject, getById, updateStatus, updateSkills
- Reads stories from project's prd.json file (stories/prd.json)
- Writes status and skills updates back to prd.json
- Status transition validation with allowed transitions:
  - pending -> in_progress, done
  - in_progress -> done, failed, pending
  - done -> pending
  - failed -> in_progress, pending
- Input validation with Zod schemas (storySchema, storyStatusEnum)
- TRPCError handling for NOT_FOUND, BAD_REQUEST, INTERNAL_SERVER_ERROR
- Unit tests with mocked filesystem (30 tests passing)

**Files changed:**
- src/lib/trpc/routers/stories.ts (new - stories tRPC router)
- src/lib/trpc/routers/stories.test.ts (new - 30 unit tests)
- src/lib/trpc/routers/index.ts (added stories router to appRouter)
- stories/prd.json (status updated)

**Learnings:**
- ZodError uses `.issues` (not `.errors`) to access validation errors array
- ZodIssue type must be imported for proper typing in error messages
- prd.json read/write pattern: read full file, modify in memory, write entire file back
- Status transition validation using a validTransitions map is clean and maintainable
- Stories are identified by string IDs (e.g., "API-004"), not database integers
- Use writeFile with JSON.stringify(data, null, 2) + '\n' for pretty-printed JSON with trailing newline
- Unit tests with SQLite must run with --no-file-parallelism to avoid concurrent access issues
- Mock both node:fs (existsSync) and node:fs/promises (readFile, writeFile) separately

## 2026-02-06 - API-005
**Title:** Skills API routes

**Implemented:**
- Created tRPC router at src/lib/trpc/routers/skills.ts for skills management
- Procedures: listCentral, listByProject, getById, createOverride, updateOverride, deleteOverride, diff
- Central skills loaded from SKILLS_PATH env var (./skills by default)
- Project overrides stored in {projectPath}/.ralph/skills/
- Skills are folders with SKILL.md file containing YAML frontmatter (name, description)
- Diff endpoint compares override content with central skill
- Integrated skills router into app router
- Unit tests with mocked filesystem (29 tests passing)

**Files changed:**
- src/lib/trpc/routers/skills.ts (new - skills tRPC router)
- src/lib/trpc/routers/skills.test.ts (new - 29 unit tests)
- src/lib/trpc/routers/index.ts (added skills router to appRouter)
- stories/prd.json (status updated)

**Learnings:**
- Claude Code skills are folders with a SKILL.md file containing YAML frontmatter (name, description)
- Frontmatter is parsed with simple regex: /^---\n([\s\S]*?)\n---\n?([\s\S]*)$/
- Simple YAML parsing works for key: value pairs without complex nesting
- When mocking existsSync with custom logic, be careful with substring matches (e.g., "invalid-skill" contains "valid-skill")
- Use PathLike type for fs mocks to avoid TypeScript errors, then convert to string inside mock
- Test isolation issues can occur when running multiple test files with shared database - each file passes individually
- Skills directory structure: {SKILLS_PATH}/{skill-id}/SKILL.md for central, {projectPath}/.ralph/skills/{skill-id}/SKILL.md for overrides
- Services in src/lib/services/ folder for business logic (runnerManager.ts)
- Docker container management via exec + promisify pattern for async CLI operations

## 2026-02-06 - API-006
**Title:** Runner API routes

**Implemented:**
- Created RunnerManager service at src/lib/services/runnerManager.ts for Docker container lifecycle
- Singleton pattern with in-memory state tracking of running containers
- Container operations: start (spawn), stop (graceful/force), getStatus, getAllStatus, cleanupOrphanedContainers
- Created tRPC router at src/lib/trpc/routers/runner.ts with full CRUD operations
- Procedures: start, stop, getStatus, getAllStatus
- Start spawns Claude Docker container via `docker run -d` with detached mode
- Stop uses `docker stop` (graceful) or `docker kill` (force) followed by `docker rm`
- Status returns: idle, running, or stopping + optional storyId, containerId, startedAt
- Validates project exists in database before container operations
- Integrated runner router into app router
- Unit tests with mocked promisify for Docker CLI (32 tests total: 13 service + 19 router)

**Files changed:**
- src/lib/services/runnerManager.ts (new - Docker container management service)
- src/lib/services/runnerManager.test.ts (new - 13 unit tests)
- src/lib/trpc/routers/runner.ts (new - runner tRPC router)
- src/lib/trpc/routers/runner.test.ts (new - 19 unit tests)
- src/lib/trpc/routers/index.ts (added runner router to appRouter)
- stories/prd.json (status updated)

**Learnings:**
- Use promisify(exec) for async Docker CLI operations instead of callback-based exec
- Mock node:util's promisify to return a controlled mock function for testing
- Container names use pattern: claude-runner-{projectId} for easy identification
- Detached mode (-d) in docker run returns container ID for tracking
- Docker ps --format with filters enables precise container existence/status checks
- In-memory Map tracking with Docker CLI verification provides reliable state management
- Singleton pattern works well for cross-request container state tracking
- getRelativeProjectPath extracts project folder name from absolute path for container mounting
- HOST_PROJECTS_ROOT env var needed for host path resolution in spawned containers
- path.join() strips leading ./ from paths - use consistent path formats in tests

## 2026-02-06 - API-007
**Title:** Project discovery service

**Implemented:**
- Created src/lib/services/projectDiscovery.ts with project discovery logic
- discoverProjects() scans PROJECTS_ROOT for folders with stories/prd.json
- Returns DiscoveredProject with path, name, description, branchName, hasPrdJson
- isValidProjectPath() helper to check if a path is a valid project
- Added projects.discover tRPC procedure that marks which projects are already in database
- Unit tests for projectDiscovery service (15 tests)
- Added discover endpoint tests to projects router tests (5 new tests)

**Files changed:**
- src/lib/services/projectDiscovery.ts (new)
- src/lib/services/projectDiscovery.test.ts (new - 15 unit tests)
- src/lib/trpc/routers/projects.ts (added discover procedure and import)
- src/lib/trpc/routers/projects.test.ts (added 5 discover tests)
- stories/prd.json (status updated)

**Learnings:**
- path.join() in Node.js strips leading ./ from paths, so './projects' joined with 'folder' becomes 'projects/folder'
- When testing with mocked existsSync, use .includes() for path matching to handle path format variations
- String ordering matters in mock conditions - check 'not-added-project' before 'added-project' to avoid substring matching issues
- Use PathLike type for mock function parameters to satisfy TypeScript in test files
- Service exports discoverProjects (async, returns DiscoveryResult) and isValidProjectPath (sync, returns boolean)
- Layout component in __root.tsx wraps children for consistent layout across routes
- Sidebar component uses useRouterState() to get current pathname for active state
- useSidebar hook manages collapsed state with localStorage persistence
- Dark mode uses matchMedia('prefers-color-scheme: dark') with addEventListener for live updates

## 2026-02-06 - UI-001
**Title:** Layout en navigatie

**Implemented:**
- Created Sidebar component with permanent desktop sidebar and mobile overlay
- Created Layout component that wraps children with sidebar
- Navigation items: Dashboard (/), Brainstorm (/brainstorm), Prompts (/prompts)
- Active state with visual indicator (left border highlight on active nav item)
- Responsive: mobile has hamburger menu + overlay sidebar, desktop has permanent sidebar
- Sidebar collapse toggle with localStorage persistence
- Dark mode support via system preference (prefers-color-scheme)
- Added distinctive typography: Plus Jakarta Sans (body) + JetBrains Mono (code)
- 28 unit tests covering navigation state, collapse toggle, responsive behavior, dark mode

**Files changed:**
- src/components/Sidebar.tsx (new)
- src/components/Sidebar.test.tsx (new - 16 tests)
- src/components/Layout.tsx (new)
- src/components/Layout.test.tsx (new - 12 tests)
- src/routes/__root.tsx (replaced Header with Layout)
- src/styles.css (added custom fonts, updated body font family)
- stories/prd.json (status updated)

**Learnings:**
- TanStack Router's useRouterState() hook provides current pathname via location.pathname
- Use cn() utility for conditional className merging with clsx + tailwind-merge
- Sidebar collapsed state persists in localStorage for user preference
- Dark mode toggle uses matchMedia listener for live system preference changes
- When testing with mocked Link component, use getAllByTestId for elements with same data-testid
- TailwindCSS v4 uses @import 'tailwindcss' instead of @tailwind directives
- Google Fonts can be imported directly in CSS via @import url()

## 2026-02-06 - UI-002
**Title:** Dashboard pagina

**Implemented:**
- Dashboard page with project cards grid
- ProjectCard component with name, description, runner status badge, progress bar
- Stats row showing done/in-progress/failed/pending counts and total stories
- Last activity timestamp with relative time formatting
- Link cards to project detail page (/project/$id)
- Add Project and Discover buttons in header
- Empty state component when no projects exist
- Stub for project detail route (to be implemented in UI-005)
- Uses tRPC queries: projects.list, stories.listByProject, runner.getStatus

**Files changed:**
- src/routes/index.tsx (replaced TanStack demo with Dashboard)
- src/routes/project/$id.tsx (new - stub for project detail page)
- src/routeTree.gen.ts (auto-generated with new route)
- stories/prd.json (status updated)

**Learnings:**
- TanStack Router file-based routing: create folder + $param.tsx for dynamic routes
- Route tree is auto-generated when dev server runs, picks up new routes
- trpc hooks (useQuery) integrate seamlessly with components
- superjson handles Date serialization from API, dates come as Date objects
- Project cards fetch their own stories/runner data for isolated data fetching
- staleTime option controls how long data is considered fresh before refetching
- Link component from TanStack Router uses `to` prop with params object for dynamic routes

## 2026-02-06 - UI-003
**Title:** Project toevoegen modal

**Implemented:**
- Created AddProjectModal component with form for path and name inputs
- Added validatePath tRPC endpoint to projects router for real-time validation
- Path validation checks: path exists, contains prd.json, not already in database
- Auto-suggests project name from prd.json metadata
- Debounced input (300ms) for path validation to reduce API calls
- Visual feedback with icons: loading spinner, checkmark, error icons
- Error messages for each validation state (path not found, no prd.json, already added)
- Keyboard support: Escape to close modal, auto-focus on path input
- Integrated modal with dashboard page via isAddModalOpen state
- Invalidates projects.list query on successful add to refresh dashboard

**Files changed:**
- src/components/AddProjectModal.tsx (new - modal component with form)
- src/lib/trpc/routers/projects.ts (added validatePath endpoint)
- src/lib/trpc/routers/projects.test.ts (added 5 tests for validatePath)
- src/routes/index.tsx (integrated modal, added state and handlers)
- stories/prd.json (status updated)

**Learnings:**
- trpc.useUtils() provides access to invalidate queries after mutations
- Debounce pattern: use useState for immediate value, useEffect with timeout for debounced value
- tRPC query with enabled: false initially disables the query until condition is met
- Focus management: use useRef + useEffect with small timeout to ensure modal is rendered before focusing
- Modal accessibility: role="dialog", aria-modal="true", aria-labelledby for title
- Validation state machine: null -> loading -> valid/path_not_found/no_prd/already_added
- isValidProjectPath from projectDiscovery service can be reused in router endpoints

## 2026-02-06 - UI-004
**Title:** Project discovery modal

**Implemented:**
- Created DiscoverProjectsModal component for bulk-adding discovered projects
- Modal lists all projects found in PROJECTS_ROOT with prd.json files
- Checkbox selection for projects with Select All / Deselect All toggle
- Already-added projects shown as disabled with "Added" badge
- Bulk add button adds all selected projects sequentially
- Loading spinner during discovery phase
- Error handling with retry capability
- Integrated modal with Dashboard page via "Discover" button
- Query refetch (staleTime: 0) ensures fresh data when modal opens

**Files changed:**
- src/components/DiscoverProjectsModal.tsx (new - 350+ lines)
- src/routes/index.tsx (added modal integration, state handlers)
- stories/prd.json (status updated)

**Learnings:**
- staleTime: 0 forces query refetch on every component mount (useful for discovery)
- Use refetch() from useQuery for manual re-triggering
- Set isAdding state during bulk operations to prevent modal close
- Sequential mutation calls for bulk add (mutateAsync with try/catch in loop)
- Group available and already-added projects in separate sections for clarity
- Use label element wrapping checkbox for better click target

## 2026-02-06 - UI-005
**Title:** Project pagina

**Implemented:**
- Project detail page at /project/:id with full feature implementation
- Project header with name and description from database
- Settings section with:
  - Project path (read-only)
  - Branch name (editable with inline edit UI)
- Story statistics section showing counts for: completed, in progress, failed, pending
- Progress bar with percentage calculation from stories
- Runner controls with start/stop buttons and live status indicator
- Quick links to Kanban board (/project/:id/kanban) and Project Prompts (/project/:id/prompts)
- Error state for non-existent projects
- Loading state while fetching data
- Created stub routes for kanban and prompts pages (to be implemented in future stories)

**Files changed:**
- src/routes/project/$id.tsx (complete rewrite - project detail page)
- src/routes/project/$id/kanban.tsx (new - stub for UI-006)
- src/routes/project/$id/prompts.tsx (new - stub for UI-015)
- src/routeTree.gen.ts (auto-generated with new routes)
- stories/prd.json (status updated)

**Learnings:**
- TanStack Router nested routes: create folder with $id/ and child files for sub-routes
- Route tree auto-generates when dev server picks up new route files
- Runner status uses refetchInterval for polling live status updates
- trpc.useUtils() provides invalidate methods for cache updates after mutations
- Inline edit pattern: useState for editing state, editValue state, save/cancel handlers
- Use enabled option in useQuery to conditionally disable queries (e.g., when projectId is NaN)
- Runner status types: 'idle' | 'running' | 'stopping' match the backend RunnerState

## 2026-02-06 - UI-006
**Title:** Kanban board pagina

**Implemented:**
- Full Kanban board page at /project/:id/kanban with 5 columns
- Columns: Backlog, Te doen, Gefaald (conditional), In Progress, Voltooid
- Smart column filtering logic:
  - Backlog: pending stories with unmet dependencies
  - Te doen: pending stories where all dependencies are done
  - Other columns: match by status directly
- Sticky header with project name, back navigation, stats bar and runner controls
- Runner controls (start/stop) with live status badge
- Stats bar showing done/in-progress/failed/pending counts and progress %
- Column headers with icon, title and story count badge
- Basic story cards with ID, priority badge, title and epic label
- Stories sorted by priority within columns
- Failed column conditionally rendered only when failed stories exist
- Responsive layout with horizontal scroll for many columns

**Files changed:**
- src/routes/project/$id/kanban.tsx (complete rewrite from stub)
- stories/prd.json (status updated to done)

**Learnings:**
- Kanban columns can have different filtering logic for same status (backlog vs todo both use 'pending' but filter by dependency status)
- Use column.id for identification since status can overlap (backlog/todo both pending)
- Stories with empty dependency arrays pass .every() check (vacuous truth)
- Conditional column visibility: filter KANBAN_COLUMNS array based on hasFailedStories
- Compact runner controls reuse logic from project detail page but with smaller UI
- Stats bar provides quick overview without taking too much vertical space
- max-h-[calc(100vh-300px)] allows columns to scroll independently

## 2026-02-06 - UI-007
**Title:** Kanban story cards

**Implemented:**
- Created reusable StoryCard component at src/components/StoryCard.tsx
- Card displays: story ID, title, priority badge (P{n}), epic label
- Dependencies shown as small badges with link icon at bottom of card
- Failed badge (red) displayed when story status is 'failed'
- Failed stories get red-tinted background and border styling
- Click handler with keyboard accessibility (Enter/Space keys)
- button role and tabIndex for clickable cards
- Integrated StoryCard into Kanban board with onClick handler for future UI-009 modal
- 27 unit tests covering all card states and interactions

**Files changed:**
- src/components/StoryCard.tsx (new - reusable story card component)
- src/components/StoryCard.test.tsx (new - 27 unit tests)
- src/routes/project/$id/kanban.tsx (updated to use StoryCard component)
- stories/prd.json (status updated to done)

**Learnings:**
- Extract reusable components to src/components/ for better maintainability
- Use data-testid attributes for reliable test selectors
- Keyboard accessibility: onKeyDown with Enter/Space for clickable divs
- Conditional role="button" and tabIndex={0} only when onClick is provided
- Use cn() utility for conditional class merging (failed state styling)
- Export type definitions alongside component for type-safe imports
- Lucide React Link icon renamed to LinkIcon to avoid conflict with TanStack Router Link
- @dnd-kit for drag & drop: DndContext wraps the board, useDraggable/useDroppable for items/containers
- PointerSensor with distance constraint (8px) prevents accidental drags on click
- DragOverlay provides a floating preview of the dragged item
- pointerWithin collision detection works well for column-based layouts
- Visual feedback: isOver + canDrop states for ring colors (primary=valid, destructive=invalid)

## 2026-02-06 - UI-008
**Title:** Kanban drag & drop

**Implemented:**
- Added @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities packages
- Created DraggableStoryCard component with useDraggable hook
- Created DroppableColumn component with useDroppable hook
- Implemented transition validation: only Backlog <-> Te doen allowed
- Block stories with unmet dependencies from moving to Te doen
- Added visual feedback during drag:
  - Primary ring for valid drop zones
  - Destructive ring for invalid drop zones
  - Background color changes on hover
  - DragOverlay with rotation and shadow for preview
- PointerSensor with 8px distance constraint to allow clicks
- 27 unit tests covering all drag validation logic

**Files changed:**
- package.json (added @dnd-kit dependencies)
- pnpm-lock.yaml (updated)
- src/routes/project/$id/kanban.tsx (major rewrite with DnD)
- src/routes/project/$id/kanban.test.tsx (new - 27 tests)
- stories/prd.json (status updated to done)

**Learnings:**
- @dnd-kit separates drag (useDraggable) from drop (useDroppable) concerns
- DragOverlay is rendered outside the normal DOM tree for smooth animations
- Column-based Kanban uses column IDs as droppable IDs, story IDs as draggable IDs
- Transition validation logic is computed at both UI (visual feedback) and drop (action) time
- Both Backlog and Te doen columns have status 'pending' - difference is dependency resolution
- Since status doesn't change, no API call is needed for these transitions
- Test helper functions extracted separately from component for isolated testing
- PointerSensor's distance constraint prevents drag activation on simple clicks

